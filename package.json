{
  "name": "mediagent-netlify",
  "version": "2.0.0",
  "description": "MediAgent — Agentic AI Doctor Booking (Netlify deployment)",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed — static HTML served directly'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}